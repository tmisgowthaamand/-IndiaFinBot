services:
  - type: web
    name: accbot-india-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: false
    envVars:
      - key: PORT
        value: 5000
      - key: NODE_ENV
        value: production
